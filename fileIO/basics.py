# read -> cursor moves to end
# when no data to read =< prints next line 
# append- a
# write- overwrite
# a,w- if not exists file new file created but not in read mode
# x=> create new file and open for writin
# w+ file is truncated
# r+ for both reading and writing but not truncate but it overwrites from begin
# a+ read write no trucate pointer at end
# readline()-> one line and then print empty next line
# with syntax
# close is no needed with "with"
# os.remove


